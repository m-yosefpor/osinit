# upgrade OS first

xcode-select --install
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install --cask \
	visual-studio-code \ # antigravity
	clipy \
	google-chrome \
        rectangle \
        ghostty \ #iterm2 \
        stats \
	vlc

##### TODO
# gnu alternatives
PATH=$(brew --prefix)/opt/findutils/libexec/gnubin:$PATH
brew install \
        starship antidote \
	iproute2mac uutils-coreutils wget findutils rar bind\ # linux
	nmap htop jq yq \ # extra linux
	fd ripgrep git-delta bottom fzf gdu \ # re-linux
	golang nvm kotlin\ # langs
	sops gopass gpg git-crypt\ # secret
	kubectl kubectx krew helm helmfile kustomize operator-sdk kubebuilder argocd kyverno\ #k8s
        gh terraform vault \
        # git-chglog/git-chglog/git-chglog caarlos0/tap/svu \ # custom taps
	kubeconform kube-score golangci-lint tflint yamllint # lint

# install sdkman java
curl -s "https://get.sdkman.io" | bash

#### (fzf) ??
git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
~/.fzf/install --all
chsh -s $(which zsh)


######
ln -sf ~/hi/zshrc ~/.zshrc
ln -s ~/hi/zsh_plugins.txt ~/.zsh_plugins.txt
ln -s ~/hi/starship.toml ~/.config/starship.toml
ln -sf ~/hi/gitconfig ~/.gitconfig

ln -sf ~/data/snapp/cred/config  ~/.ssh/config

######### rectangle
maximize: window+up
left half: window left
right half: window right
de-maximize

###############################
preference > dock >
		hide dock
		battery > show percent


perefrences > keyboard > shortcut > check input source ctrl+space
			keyboard setting "perisian-standard" not "persian"
                        also add US , and remove US-international
			> use F keys as default function

###### change hostname, computername
sudo scutil --set HostName <new host name>
sudo scutil --set LocalHostName <new host name>
sudo scutil --set ComputerName <new name>
dscacheutil -flushcache
sudo reboot
